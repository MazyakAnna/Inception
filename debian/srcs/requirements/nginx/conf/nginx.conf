## ## https://wordpress.org/support/article/nginx/#per-site-configuration

# server {
#     listen 80;
#     listen [::]:80;
#     server_name bmaegan.42.fr www.bmaegan.42.fr;
#     return 301 https://$host$request_uri;
# }

server {
    listen      443 ssl;
    listen [::]:443 ssl;
    server_name  bmaegan.42.fr www.bmaegan.42.fr;

    ssl_certificate     /etc/ssl/bmaegan.42.fr.crt;
    ssl_certificate_key /etc/ssl/bmaegan.42.fr.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_session_timeout 10m;
    keepalive_timeout 70;

    root /var/www/html/wordpress;
    index index.php index.html index.htm;
    
    location / {
        try_files $uri $uri/ /index.php?$args;
        add_header Cache-Control 'no-store, no-cache';
    }
   location ~ \.php$ {
       fastcgi_split_path_info ^(.+\.php)(/.+)$;
       fastcgi_pass wordpress:9000;
       fastcgi_index index.php;
       include fastcgi_params;
       fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
       fastcgi_param PATH_INFO $fastcgi_path_info;
   }
}

server {
    listen      8090 ssl;
    listen [::]:8090 ssl;
    server_name  bmaegan.42.fr www.bmaegan.42.fr;
    ssl_certificate     /etc/ssl/bmaegan.42.fr.crt;
    ssl_certificate_key /etc/ssl/bmaegan.42.fr.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    root /var/www/html/;
    index index.php index.html index.htm;
    location / {
        try_files $uri $uri/ /index.php?$args;
    }
   location ~ \.php$ {
       fastcgi_split_path_info ^(.+\.php)(/.+)$;
       fastcgi_pass phpmyadmin:8080;
       fastcgi_index index.php;
       include fastcgi_params;
       fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
       fastcgi_param PATH_INFO $fastcgi_path_info;
   }
}

server {
    listen      8000 ssl;
    listen [::]:8000 ssl;
    server_name  bmaegan.42.fr www.bmaegan.42.fr;

    ssl_certificate     /etc/ssl/bmaegan.42.fr.crt;
    ssl_certificate_key /etc/ssl/bmaegan.42.fr.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    root /var/www/html/;
    index index.php index.html index.htm;
    location / {
        try_files $uri $uri/ /index.php?$args;
    }
   location ~ \.php$ {
       fastcgi_split_path_info ^(.+\.php)(/.+)$;
       fastcgi_pass adminer:8080;
       fastcgi_index index.php;
       include fastcgi_params;
       fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
       fastcgi_param PATH_INFO $fastcgi_path_info;
   }
}